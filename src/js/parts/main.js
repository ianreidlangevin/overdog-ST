
jQuery(document).ready(function($) {

  /* init AOS for Animation on scroll with CSS
  * If you use Scroll Magic with Gsap - Create a js file Animation and delete AOS references in theme

  AOS.init({
    offset: 0,
    duration: 1200,
  });

  */

  /* init Parallax located in lib folder

  $('.is-parallax').parallax({
    posY: 'top',
    overScrollFix: true,
    excludeAgents: /(iPod|iPhone|iPad|Android)/,
  });
  */

});
